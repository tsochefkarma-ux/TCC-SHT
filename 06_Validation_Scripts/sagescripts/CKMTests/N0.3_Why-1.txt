cat > investigate_minus_one.sage << 'EOF'
print("="*70)
print("INVESTIGATING THE (κ-1) STRUCTURE")
print("="*70)

kappa = [9, 3, 3]
kappa_123 = 27
h21 = 3
h11 = 3

print(f"\nTopological data: κ = {kappa}, κ₁₂₃ = {kappa_123}, h = {h21}")

print("\n" + "-"*70)
print("OPTION 1: Hodge number relation")
print("-"*70)
print(f"  h²¹ - 1 = {h21 - 1}")
print(f"  κ₁ - 1 = {kappa[0] - 1}")
print(f"  κ₂ + κ₃ - 1 = {kappa[1] + kappa[2] - 1}")
print(f"  No obvious Hodge connection...")

print("\n" + "-"*70)
print("OPTION 2: Quotient group structure")
print("-"*70)
print(f"  Z₃ × Z₃ has order 9")
print(f"  Z₃ has order 3")
print(f"  9 - 1 = 8 (non-identity elements of Z₃×Z₃)")
print(f"  3 + 3 - 1 = 5 (??? doesn't match group theory)")

print("\n" + "-"*70)
print("OPTION 3: Effective dimensions")
print("-"*70)
print(f"  Total moduli: h²¹ = {h21}")
print(f"  If one direction is 'fixed' by symmetry...")
print(f"  Effective moduli = h²¹ - 1 = {h21 - 1}")

print("\n" + "-"*70)
print("OPTION 4: Intersection form structure")
print("-"*70)
# The full intersection tensor has components κᵢⱼₖ
# For diagonal: κ₁₁₁ = 9, κ₂₂₂ = 3, κ₃₃₃ = 3
# What about off-diagonal?
# For Schoen with Z₃×Z₃: κ₁₂₃ = 27

# Maybe the "-1" relates to trace vs traceless part?
trace = kappa[0] + kappa[1] + kappa[2]
print(f"  Tr(κ) = κ₁ + κ₂ + κ₃ = {trace}")
print(f"  κ₁₂₃ - Tr(κ) = {kappa_123 - trace} (not 8 or 5)")

print("\n" + "-"*70)
print("OPTION 5: Check if 8/5 appears elsewhere")
print("-"*70)
# Look for 8/5 in various combinations
print(f"  (κ₁₂₃ - κ₁)/κ₁₂₃ = {(kappa_123 - kappa[0])/kappa_123}")
print(f"  (κ₁₂₃ - κ₂ - κ₃)/κ₁ = {(kappa_123 - kappa[1] - kappa[2])/kappa[0]}")
print(f"  κ₁/(κ₁ + 1/9) = doesn't make sense")

# What if 8/5 = 1.6 relates to sqrt(3)?
print(f"\n  √(8/5) = {float(sqrt(8/5)):.4f}")
print(f"  √3 × (something) = {float(sqrt(3)):.4f}")
print(f"  √(8/5) / √3 = {float(sqrt(8/5)/sqrt(3)):.4f}")

print("\n" + "-"*70)
print("OPTION 6: Direct geometric interpretation")
print("-"*70)
# The intersection numbers count curves
# κᵢᵢᵢ = self-intersection of i-th divisor class
# κ₁₂₃ = triple intersection

print(f"  κ₁ = 9 counts... (D₁ · D₁ · D₁)")
print(f"  κ₂ = κ₃ = 3 counts... (D₂/₃ · D₂/₃ · D₂/₃)")
print(f"  κ₁₂₃ = 27 counts... (D₁ · D₂ · D₃)")

# Fixed point counting under Z₃
print(f"\n  Under Z₃ action:")
print(f"  9 = 3² fixed points on first factor?")
print(f"  9 - 1 = 8 = non-fixed?")

print("\n" + "-"*70)
print("OPTION 7: Check alternate formula for η")
print("-"*70)
# Maybe there's a cleaner expression?
# η̄_obs = 0.348
A = float((-25 + sqrt(653))/2)
eta_obs = 0.348

# Try various combinations
print(f"  Observed η̄ = {eta_obs}")
print(f"  A × √(8/5) = {A * float(sqrt(8/5)):.4f}")
print(f"  A × √(κ₁/κ₂ - 1) = {A * float(sqrt(kappa[0]/kappa[1] - 1)):.4f}")
print(f"  A × √(κ₁/κ₂ - 1) = A × √2 = {A * float(sqrt(2)):.4f}")

# Aha! Check this
print(f"\n*** CHECKING: Is 8/5 = (κ₁-κ₂)/κ₂? ***")
print(f"  (κ₁ - κ₂)/κ₂ = {(kappa[0] - kappa[1])/kappa[1]} = {float((kappa[0] - kappa[1])/kappa[1])}")
print(f"  But 8/5 = {8/5}")
# No, (9-3)/3 = 2 ≠ 8/5

print(f"\n*** CHECKING: Is 8/5 related to eigenvalue ratio? ***")
lambda_1 = float(sqrt(A))
lambda_2 = lambda_1 * float(sqrt(3))
print(f"  λ₂²/λ₁² = 3")
print(f"  (λ₂² - λ₁²)/λ₁² = 2")

print("\n" + "-"*70)
print("OPTION 8: The '-1' as trace subtraction")  
print("-"*70)
# In matrix theory, traceless parts often appear
# If we have a 3×3 structure...
print(f"  Consider the 'anisotropy' in each direction:")
print(f"  Direction 1: κ₁ = 9, mean = (κ₁+κ₂+κ₃)/3 = {(kappa[0]+kappa[1]+kappa[2])/3}")
print(f"  Deviation: κ₁ - mean = {kappa[0] - (kappa[0]+kappa[1]+kappa[2])/3}")

# Hmm, let me try a different approach
print(f"\n  Traceless approach:")
print(f"  κ₁ - (κ₂+κ₃)/2 = {kappa[0] - (kappa[1]+kappa[2])/2}")
print(f"  This equals 6, not 8")

print("\n" + "="*70)
print("MOST LIKELY EXPLANATION")
print("="*70)
print("""
The "-1" may represent:

1. GAUGE FIXING: In moduli space, one direction is often fixed by
   overall volume/scale. Effective DoF = dimension - 1.
   
2. Z₃×Z₃ STRUCTURE: The group has 9 elements, but the identity
   contributes trivially. "Interesting" elements = 9 - 1 = 8.
   Similarly for Z₃+Z₃ factors: 3+3-1 = 5 (overlap of identity).

3. PROJECTIVE EFFECT: In projective geometry, one always subtracts 1
   from the ambient dimension. Could relate to CP² base structure.

The formula WORKS to sub-1%, suggesting it's not coincidence.
But the geometric derivation needs more work.
""")
EOF
sage investigate_minus_one.sage